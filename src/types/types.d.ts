interface BgInfo {
  children: React.ReactNode;
  height: string;
  text: string;
}

interface Button {
  mainText: string;
  bgColor: string;
  textColor: string;
}

interface Input {
  type: string;
  placeholder: string;
}

interface Content {
  height: string;
  children: React.ReactNode;
}

interface Agree {
  children: React.ReactNode;
}
